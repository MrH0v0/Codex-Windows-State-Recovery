interface:
  display_name: "Codex State Recovery"
  short_description: "Safely inspect and recover Codex projects and local state"
  default_prompt: "Use $repair-codex-windows-state to inspect this Codex update issue read-only, then choose a reversible recovery path."
